name: Backup diario a buckets (R2 + B2)

# Corre pg_dump contra Supabase y sube el resultado comprimido a los dos buckets de backup
# (Cloudflare R2 como principal, Backblaze B2 como espejo) — ver docs/PLAN_HASTA_PRODUCCION.md y
# docs/SECURITY.md, sección de backup propio independiente del nativo de Supabase.
#
# Y después copia los archivos de los depósitos, que hasta hoy no se respaldaban: una base
# restaurada sin ellos dice que el certificado está cargado y el certificado no vuelve.
on:
  schedule:
    - cron: '0 6 * * *' # 03:00 America/Argentina/Buenos_Aires (UTC-3)
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Instalar cliente de Postgres 17 (pg_dump)
        # El servidor de Supabase corre Postgres 17.6 — pg_dump exige que su versión sea
        # >= la del servidor. El repo apt por defecto de Ubuntu solo trae la 16, así que se
        # agrega el repo oficial de PostgreSQL (apt.postgresql.org) para instalar la 17.
        run: |
          sudo sh -c 'echo "deb https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
          curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/pgdg.gpg
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17

      - uses: actions/setup-node@v4
        with:
          # 22 y no 20: el cliente de Supabase necesita el WebSocket nativo, que en 20 no existe.
          # Con 20 el volcado de la base salía bien y la copia de los archivos moría siempre.
          node-version: '22'

      - name: Instalar dependencias
        working-directory: backend
        # `ci` y no `install`: install ignora el candado de versiones y se trae la última de cada
        # biblioteca. Así entró una versión del cliente de Supabase que rechaza la llave de
        # servicio, y la copia de los archivos murió con un error que no tenía nada que ver.
        run: npm ci --omit=dev

      - name: Ejecutar backup
        working-directory: backend
        env:
          DB_HOST: ${{ secrets.BACKUP_DB_HOST }}
          DB_PORT: ${{ secrets.BACKUP_DB_PORT }}
          DB_NAME: ${{ secrets.BACKUP_DB_NAME }}
          DB_USER: ${{ secrets.BACKUP_DB_USER }}
          DB_PASSWORD: ${{ secrets.BACKUP_DB_PASSWORD }}
          # Los archivos no viajan en el volcado: se bajan del almacenamiento, y para eso hace
          # falta la llave de servicio (docs/SECRETOS.md). La dirección es la misma que usan las
          # pantallas y no es secreta, así que se reusa la que ya está cargada en vez de pedir
          # que se cargue el mismo valor dos veces con dos nombres.
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          B2_ENDPOINT: ${{ secrets.B2_ENDPOINT }}
          B2_KEY_ID: ${{ secrets.B2_KEY_ID }}
          B2_APPLICATION_KEY: ${{ secrets.B2_APPLICATION_KEY }}
          B2_BUCKET: ${{ secrets.B2_BUCKET }}
        run: node scripts/backup_a_buckets.mjs
